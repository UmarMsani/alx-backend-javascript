/**
 * Creates a new Set from an array.
 * @param {Array} arr - The input array to create a Set from.
 * @returns {Set} - A Set containing unique elements from the input array.
 */
const setFromArray = (arr) => new Set(arr);

export default setFromArray;
